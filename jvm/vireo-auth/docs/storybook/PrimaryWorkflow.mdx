import { Meta, Source } from "@storybook/addon-docs/blocks";
import configurationSource from "../../../vireo-starter-documentation-examples/src/main/java/com/vireocode/docs/auth/AuthSecurityConfigurationExample.java?raw";

<Meta title="JVM/Auth/Primary Workflow" />

# Primary workflow

The default dependency secures `/api/**`. Add application-specific public or restricted routes through a customizer when the session model remains appropriate.

## Why it exists

Copying the complete starter security chain to add one route forfeits future security fixes. `StarterHttpSecurityCustomizer` keeps the application-owned rule explicit while retaining Vireo's session, CSRF, error, and endpoint defaults. If an application must add a narrower `SecurityFilterChain`, its browser chain can delegate to `StarterSessionSecurity` instead of copying that policy.

The exact source below is compiled by `vireo-starter-documentation-examples` during the JVM build.

<Source language="java" code={configurationSource} />
